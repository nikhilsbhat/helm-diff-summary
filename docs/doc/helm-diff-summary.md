## helm-diff-summary

A Terraform-style summarizer for helm diff output

### Synopsis

A utility that reads the helm diff plugin's output and summarizes its output in a Terraform style

```
helm-diff-summary [command] [flags]
```

### Examples

```
helm diff ... --output diff | helm-diff-summary
helm diff upgrade sample ../helm-images/example/chart/sample  | ./helm-diff-summary -o yaml
helm diff upgrade sample ../helm-images/example/chart/sample  | ./helm-diff-summary -o json
helm diff upgrade sample ../helm-images/example/chart/sample  | ./helm-diff-summary --fail-on high
helm diff upgrade sample ../helm-images/example/chart/sample  | ./helm-diff-summary --fail-on-delete
helm diff upgrade sample ../helm-images/example/chart/sample  | ./helm-diff-summary --notify slack,gchat
```

### Options

```
      --fail-on string     fail when violations reach severity: low|medium|high|critical
      --fail-on-delete     when set, fail if deletes are detected
  -h, --help               help for helm-diff-summary
  -l, --log-level string   log level for the helm-diff-summary (default "INFO")
      --no-color           when set, renders the output with no color
      --notify strings     name of the notifiers to send the report to ex: slack,teams
  -o, --output string      output format to which the output needs to be rendered
  -v, --version            when set, prints the version of the utility
```

###### Auto generated by spf13/cobra on 18-May-2026
